{
  "name": "seguros-saas",
  "private": true,
  "version": "0.1.0",
  "description": "Sistema de RegulaÃ§Ã£o e GestÃ£o de Sinistros â€” monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "npm run generate --workspace=@seguros/database",
    "db:seed": "npm run db:seed --workspace=@seguros/database",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "prepare": "husky install || exit 0"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "prettier": "^3.3.3",
    "turbo": "^2.1.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.8.0",
  "dependencies": {
    "lucide-react": "^0.446.0"
  }
}

